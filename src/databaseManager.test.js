/*
TO DO: mock the module used for connections
const { setQueue, test, run, addToDB } = require('./databaseManager')

test('setQueue should be defined', () => {
  expect(setQueue).toBeDefined()
})

test('test should be defined', () => {
  expect(test).toBeDefined()
})

test('run should be defined', () => {
  expect(run).toBeDefined()
})

test('addToDB should be defined', () => {
  expect(addToDB).toBeDefined()
})
*/
